# Cloned repositories (managed via repos.conf)
teiserver/
bar-lobby/
spads_config_bar/
ansible-spads-setup/
Beyond-All-Reason
BYAR-Chobby
bar-db/
bar-live-services/
RecoilEngine
lua-doc-extractor
SPADS/
SpringLobbyInterface/

#spads cache data (empty folder to reset spads install)
spads_data/

# Personal config overrides
repos.local.conf
.env

# Runtime / editor state
tasks/
.devtools/
*.log
